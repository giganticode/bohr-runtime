-n {{task.name}}_label_dataset

-d {{config.paths.generated_dir}}/{{task.name}}/heuristic_matrix_train.pkl
{% for datapath in task.train_datapaths %} -d {{datapath}}{% endfor %}
{% for dataloader in task.train_datasets %} -d {{dataloader|replace('.', '/')}}.py{% endfor %}

-p bohr.json:bohr_framework_version

-o {{config.paths.labeled_data_dir}}/{{task.name}}.csv

bohr label-dataset {{task.name}} {{task.train_datasets|first}}